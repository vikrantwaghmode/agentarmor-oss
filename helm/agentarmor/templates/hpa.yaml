{{- if .Values.ha.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "agentarmor.fullname" . }}
  labels:
    {{- include "agentarmor.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "agentarmor.fullname" . }}
  minReplicas: {{ .Values.ha.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.ha.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.ha.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
