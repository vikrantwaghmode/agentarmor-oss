# firewall.yaml
# Only these domains will be allowed to pass through the firewall.
allowed_domains:
  - "api.openai.com"
  - "api.anthropic.com"
  - "generativelanguage.googleapis.com" # The primary Gemini API domain
  # Uncomment to enable sidecar services (Docker-internal traffic):
  - "presidio-analyzer"   # Presidio PII scanner
  - "ollama"              # LLM-powered prompt injection scanner